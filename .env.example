# Docker Compose environment variables
# Copy this file to .env and fill in your values

# PostgreSQL password (must match the password in config.json database URL)
POSTGRES_PASSWORD=changeme

# Optional: Override the web dashboard port mapping
# WEB_PORT=8000
